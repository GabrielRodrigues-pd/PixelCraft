import { styled } from "../stitches.config";

export const Nav = styled("nav", {
  display: "flex",
  margin: "0 auto",
  justifyContent: "space-between",
  width: "100%",
  padding: "0 11.2rem",
});
